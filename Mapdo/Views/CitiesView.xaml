<?xml version="1.0" encoding="utf-8" ?>
<this:ViewPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Mapdo.ViewModels"
             xmlns:this="clr-namespace:Mapdo"
             x:Class="Mapdo.Views.CitiesView"
             x:TypeArguments="vm:CitiesViewModel"
             Title="Cities">
  
    <ContentPage.ToolbarItems>
      <ToolbarItem Name="Add City" Order="Primary" Priority="0" Activated="OnAdd" />
    </ContentPage.ToolbarItems>

    <ListView x:Name="list" ItemsSource="{Binding Cities}" ItemTapped="OnItemTapped">
        <ListView.ItemTemplate>
            <DataTemplate>
              <TextCell Text="{Binding Name}">
                <TextCell.ContextActions>
                  <MenuItem Text="Delete" IsDestructive="true" Clicked="OnMenuItemDeleteClicked" />
                </TextCell.ContextActions>
              </TextCell>


              <!--var menuItemMore = new MenuItem();
              menuItemMore.Text = "Options";
              menuItemMore.IsDestructive = false;
              menuItemMore.Clicked += OnMenuItemMoreClicked;
              ContextActions.Add(menuItemMore);-->

            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    
</this:ViewPage>