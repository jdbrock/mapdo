<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:this="clr-namespace:Mapdo"
             x:Class="Mapdo.PoiInformationView"
             Title="{Binding Poi.Name}">

  <StackLayout Padding="20" Spacing="5">
    <Label Text="{Binding Poi.Name}" VerticalOptions="Center" HorizontalOptions="Start" FontSize="24" />
    <Label Text="{Binding Poi.ExternalYelpData.DisplayAddress, StringFormat='Address: {0}'}" VerticalOptions="Center" HorizontalOptions="Start" FontSize="14" />
    <Label Text="{Binding Poi.ExternalYelpData.display_phone, StringFormat='Phone: {0}'}" VerticalOptions="Center" HorizontalOptions="Start" FontSize="14" />
    <Label Text="{Binding Poi.ExternalYelpData.rating, StringFormat='Rating: {0}/5'}" VerticalOptions="Center" HorizontalOptions="Start" FontSize="14" />

    <ListView ItemsSource="{Binding Poi.ExternalYelpData.reviews}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding excerpt}" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
  
</ContentPage>